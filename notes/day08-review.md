# Day 8 任务书：Day 7 后半补完 + 周复盘（**测速点**）

> **日期**：2026-09-21（周一）
> ⚠️ **今天被课切成三段**：9:00–11:50、13:30–16:50 上课，期间几乎用不了电脑。
> 所以今天的设计是「**课间读、放学做**」：不用电脑的部分（阅读、纸上默写）放到课间；需要电脑的部分**全部排在 16:50 之后**。
> 按"选 A"：今天只排 3～4 小时的量（其中放学后约 3 小时 15 分）。
> **今天最重要的一件事**：**周复盘 = 测速点** —— 重新算账（内容日 vs 日历日），并产出「**还会的 / 忘了的**」两张清单。**这一项不许省。**

---

## 〇 今天的产出物

| # | 产出物 | 验收标准 | 谁写 |
|---|---|---|---|
| 1 | 周自测的**原始记录**（写进日志） | 三部分都做了，全程**关掉 AI** | **你** |
| 2 | `week1-language/day08-zerohint-02.js` | 零提示题第 2 道 `countChars`，按验收表 | **你（零提示）** |
| 3 | `week1-language/day08-zerohint-02-verify.js` | **你自己写的判据**（3–8 行：跑用例、打 ✅/❌、汇总） | **你** |
| 4 | `week1-language/day08-event-loop.js` | 有"**我先预测 → 再实测**"的对照输出 | **你** |
| 5 | `week1-language/day08-promise-api.js` | 四个方法各一个例子 + 一张对比表（填满） | **你** |
| 6 | `notes/week1-review.md` | **两张清单**（还会的 / 忘了的）+ 算账表 | **你** |
| 7 | `notes/day08.md` + commit + push | 三节填完 | **你** |

---

## 一 时间表（三段式）

### A 段：课前 + 课间（**不用电脑**，手机 / 纸笔，合计约 50 分钟）

| 什么时候 | 干什么 | 时长 |
|---|---|---|
| 出门前 / 课间空档 | 读 [《微任务（Microtask）》](https://zh.javascript.info/microtask-queue) —— 这就是昨天脚手架"观察 1"的谜底（为什么 `.then` 一定排在同步代码后面） | 20 min |
| 午休 11:50–13:30 | 读 [《Promise API》](https://zh.javascript.info/promise-api)（`Promise.all` / `allSettled` / `race` / `any` 的区别） | 30 min |
| 任意空档（**可选**） | 纸上默写：本周 5 个函数 `debounce` / `throttle` / `curry` / `deepClone` / 数组三兄弟 的**签名 + 一句话作用 + 各自最容易答错的一个坑** | 5 min |

> 课间读不完不要紧 —— 读多少算多少，剩下的挪到晚上（晚上"先读再写"，但见下面的检查点）。

### B 段：放学后 16:50–20:30（主块，约 3 小时 15 分）

| 时间 | 干什么 | 产出 |
|---|---|---|
| **16:50–17:50** | **① 周自测（关掉 AI）** | 产出物 1 |
| 17:50–18:00 | 休息，不碰屏幕 | —— |
| **18:00–18:40** | **② 微任务 vs 宏任务**：先预测、再跑 | 产出物 4 |
| **18:40–19:10** | **③ `Promise.all` 家族** | 产出物 5 |
| **19:10–19:30** | **④ ESM vs CJS**（只读机制，20 分钟） | 日志里 4 行笔记 |
| **19:30–20:00** | **⑤ 周复盘 + 算账** → `notes/week1-review.md` | 产出物 6 |
| **20:00–20:15** | **⑥ 日志 + commit + push** | 产出物 7 |

> **关键检查点：19:30。**
> 如果那时 ②③④ 还没做完，**就跳过没做完的**（登记进欠账表），**⑤ 周复盘不许省** —— 它是今天的测速点，是"下一步该排多少"的唯一依据。

---

## 二 任务详情

### ① 周自测（60 分钟，**关掉 AI**）—— 今天的主角

三部分，全程**不许开 AI**；除了明确允许的，也**不许翻自己写过的代码**。

#### a. 轮转复习 3 个模块（15 分钟）

```powershell
node week1-language/recall-verify.js curry
node week1-language/recall-verify.js deepClone
node week1-language/recall-verify.js throttle
```

先写 `week1-language/recall-curry.js` / `recall-deepClone.js` / `recall-throttle.js` 三个文件，**写完再跑**；跑之前别打开 `p0-toolkit/src/`。
（昨天做的是 `debounce`，按轮转表今天该换到这三个。写不出来没关系 —— **写不出来的地方就是"忘了的"清单的原料**。）

#### b. 零提示题第 2 道（25 分钟）—— **这次要"自己搭判据"**

**题目：`countChars(str)`** —— 统计字符串里**每个字符出现的次数**，返回一个对象（键 = 字符，值 = 次数）。

**验收标准：**

| 输入 | 期望输出 |
|---|---|
| `'aba'` | `{ a: 2, b: 1 }` |
| `'aaa'` | `{ a: 3 }` |
| `''` | `{}` |
| 只出现一次的字符 | 值是 `1`（不许是 `undefined`、也不许是 `0`） |
| 返回值 | 必须是**新对象** |

**这次的判据你自己写**（这就是昨天欠的那一课）：`week1-language/day08-zerohint-02-verify.js`
要求很简单 —— 3～8 行就够：
- 跑上面表格里的 4 个用例
- 每个用例打印 `✅` / `❌`（❌ 时把"实际得到了什么"也打出来）
- 最后打印一行"通过 X 项，失败 Y 项"

> **先写判据、再写实现** —— 先想清楚"怎么算对"，再动手"怎么做"。这也是为什么面试里"你怎么验证的"比"你写了什么"更能说明问题。

**规则照旧**：限时 25 分钟；卡住**只许查 MDN / 书**，不许问 AI；到点没做出来就**停手**，把"卡在哪一步"写进日志。
**这是第 1 道 `dropNulls` 的"隔天同类题"**（昨天的诊断是"学过但零提示取不出来"，所以换一道**同主题**（对象遍历 / 往对象里写键）再考一次）。
完成标准：`node week1-language/day08-zerohint-02.js` 和 `node week1-language/day08-zerohint-02-verify.js` 都能跑。

#### c. 口述 4 题（10 分钟，讲出来或写下来）

1. `this` 的四种绑定 + **箭头函数为什么例外**（Day 6 小测的坑）
2. 手写深克隆为什么处理不了**循环引用**
3. **错误优先回调**：`(err, data) => { if (err) { ...; return; } ... }` 这段在干什么、**为什么**要这么写
   （← **昨天欠的账，这条是重点**。讲不出来的话，回去读《回调》那一节 15 分钟，这一条记进「忘了的」清单）
4. 防抖和节流**分别**用在什么场景

---

### ② 微任务 vs 宏任务（40 分钟）：**先预测，再跑**

**先别跑。** 把下面这段抄进 `week1-language/day08-event-loop.js`，**在纸上写下你预测的输出顺序（1–8）**，然后再跑，把"预测 vs 实测"记在文件里当注释。

```js
console.log('1 同步开始');
setTimeout(() => console.log('2 setTimeout 0'), 0);
Promise.resolve().then(() => console.log('3 promise.then'));
process.nextTick(() => console.log('4 nextTick'));
console.log('5 同步结束');
setTimeout(() => console.log('6 setTimeout 0（第二个）'), 0);
Promise.resolve().then(() => console.log('7 promise.then（第二个）'));
process.nextTick(() => console.log('8 nextTick（第二个）'));
```

**要搞清楚的三个问题**（答案写进文件的注释里，用你自己的话）：
1. 为什么 `nextTick` 比 `promise.then` 还要早？
2. 为什么两个 `setTimeout(..., 0)` 中间**没有**插进 `promise.then`？
3. 用一句话说清"微任务队列在什么时候被清空"。

**再补一个对照实验**：把 `Promise.resolve().then(...)` 换成 `await`（写在 `async` 函数里）—— 顺序会变吗？为什么？

---

### ③ `Promise.all` 家族（30 分钟）

新建 `week1-language/day08-promise-api.js`：四个方法各写一个**最小**例子，并且**造一个"其中有一个会失败"的场景**（数组里混一个 `Promise.reject('x')` 就行），把这张表填满：

| 方法 | 全部成功时拿到什么 | 有**一个**失败时会怎样 | 什么时候用它 |
|---|---|---|---|
| `Promise.all` | | | |
| `Promise.allSettled` | | | |
| `Promise.race` | | | |
| `Promise.any` | | | |

跑起来，把**实测**结果填进去（不是"应该是"）。

---

### ④ ESM vs CJS（20 分钟，只读机制）

昨天你撞的那个现场就是这一节的入口：

```
ERR_AMBIGUOUS_MODULE_SYNTAX: Cannot determine intended module format
because both 'module' and top-level await are present.
```

读：javascript.info [《模块 (Module) 简介》](https://zh.javascript.info/modules-intro) + [《导出和导入》](https://zh.javascript.info/import-export)（快读，20 分钟）。

然后在日志里写 **4 行笔记**（就 4 行，别写多）：
1. 两套的语法差别（`module.exports` / `require` ↔ `export` / `import`）
2. 为什么"顶层 `await` 只属于 ESM"
3. `p0-toolkit` 现在用的是哪套？以后转 TS + Vitest 会变成哪套？
4. 昨天那个报错，**用一句话解释给面试官听**

---

### ⑤ 周复盘 + 算账（30 分钟）→ `notes/week1-review.md`

**必须有两张清单**（素材就是 ① 的原始记录）：

- **「还会的」**：能凭记忆写出来 / 讲得清楚的
- **「忘了的」**：写不出来、讲不清、或者"零提示取不出来"的

然后按计划 §四 的规则**重新算账**。账我先算好了，你**核对**（不对就改，写上你的理由）：

| 项 | 天数 |
|---|---|
| 9/18 那次算的缺口（剩余内容 26.5 内容日 vs 剩余日历 26 天） | 0.5 |
| 9/19–9/20 两个日历日消耗掉的内容：Day 6 欠账 0.5 + Day 7 前半约 0.5 | 1.0 |
| 新增缺口 | +1.0 |
| **累计缺口（截至 9/21 开工）** | **约 1.5 天** |
| 按规则（累计欠账每满 1 天就顺延 1 天）→ 结束日 | 10/14 → **10/15** |

**"忘了的"那张清单怎么用**：它就是**下一周轮转表和零提示题的输入**（计划 §七 心法第 9 条）。
从里面挑 **2–3 条**最想先恢复的，写进日志的「明天第一件事」。

> ⚠️ 今天这个复盘还有一件事：**决定接下来要不要加压**。
> 依据是两张清单的比例 —— 「忘了的」很短 → 可以加压（下一段内容日排到 5–6 小时）；「忘了的」很长 → **不许加压**（那只会让"学过取不出来"更严重）。
> 另外把昨天的观察也记一笔：`day07-promise.js` 花了 5 轮，其中大部分是**语法/命名/返回值**这类流程返工（不是学习内容）—— **压掉返工比延长工时更能加快总进度**。

---

### ⑥ 日志 + commit（15 分钟）

```powershell
git add -A
git commit -m "day08: event loop + promise api, week1 review (recap + recall lists)"
git push
git status -sb
```

---

## 三 今天的完成标准

- [ ] 周自测 a / b / c 三部分都做了，**全程关掉 AI**，原始记录进了日志
- [ ] `day08-zerohint-02.js` + **你自己写的** `day08-zerohint-02-verify.js` 都能跑出结果
- [ ] `day08-event-loop.js` 里有"我先预测的顺序"和"实测顺序"的对照
- [ ] `day08-promise-api.js` 跑起来了，四个方法的对比表**填的是实测结果**
- [ ] ESM/CJS 的 4 行笔记
- [ ] `notes/week1-review.md`：**两张清单** + 算账表（含结束日结论）
- [ ] `notes/day08.md` 三节 + commit + push

---

## 四 如果时间不够：砍单顺序

**保底（今天不许省）**

1. **① 周自测** —— 没有它，周复盘就是拍脑袋
2. **⑤ 周复盘 + `notes/week1-review.md`** —— 测速点，决定后面怎么排
3. 日志 + commit + push

**可以顺延（按这个顺序，从下往上砍）**

4. ④ ESM vs CJS（只读，挪走损失最小）
5. ③ `Promise.all` 家族
6. ② 微任务 vs 宏任务

**课间那部分**（阅读）如果白天没空：晚上的顺序改成"先读 30 分钟再写"，但 **⑤ 不许晚过 21:30**。

---

## 五 今天不碰什么

- **TypeScript 入门**（Day 8 原本的内容）→ 顺延到下一天
- **手写 Promise（A+ 规范）** → 第 3 周之后
- **LeetCode** → 第 2 周起
- **`process` / `Map` 等探索 TODO** → 不许插队（心法第 6 条）

---

## 六 明天的锚点

下一天 = **TypeScript 入门 + `p0-toolkit` 转 TS**（配 `tsconfig.json` + `strict: true`），并把周复盘里「**忘了的**」挑出来的 2–3 条塞进轮转表。

---

## 附：今天的"不用电脑"清单（课间翻这一块就够）

1. 《微任务（Microtask）》—— 20 min
2. 《Promise API》—— 30 min
3. 纸上默写：本周 5 个函数的签名 + 作用 + 一个坑（可选，5 min）
4. 口述 4 题（① c 部分）也可以在路上想 —— 想不出来的直接记进「忘了的」清单
