# AI 协作文档：交接 + 过程记录

> **这份文档有两个用途**
> 1. **交接**：让一个没有任何对话历史的新 AI 会话，在 5 分钟内接手这个项目
> 2. **记录**：把这段学习过程公开下来 —— 包括协作方式、走过的弯路、踩过的坑
>
> **最后更新**：2026-09-21（Day 8 = 周复盘日，开工前）
> **上一段对话的会话 ID**：`sess_0b328783-cf26-4840-b6e9-e5bb6ca7fe82`
> （新会话可用 `ReadSessionContext` 读它，但**优先读这份文件**）

### ⚠️ 两个新变化（日程被切 / Day 跨天）

**1. Day 7 跨了两个日历日**（9/19–9/20），已经做完：
- **任务书**：第 1 天 `notes/day07-async.md`（9/19）、第 2 天 **`notes/day07-day2.md`**（9/20）
- **日志**：`notes/day07.md` 里**分两段**（`# Day 7 第 1 天` / `# Day 7 第 2 天`），**不另开日志文件** —— "一个 Day 一份日志"这个约定没变，只是这份日志里有两段。
- 下次如果再有跨天的 Day，照这个办法：**任务书另开一个文件，日志往同一个文件里加一段。**

**2. 学生的日历被课切成三段时，任务书要按"能不能用电脑"来分段**（9/21 Day 8 是第一个例子）：
上午 9:00–11:50、下午 13:30–16:50 上课 → 任务书里明确写「**课间读（不用电脑）、放学做（16:50 之后）**」，并把**需要电脑的活全部排在晚上**；再埋一个**关键检查点**（如 19:30），宁可砍内容也不许把"测速点 / 周复盘"这种不可替代的环节挤掉。

### 为什么这份文档是公开的

计划书附录 D 的"一个月后的自检"里有一条：**"能说清我的代码里哪部分是 AI 帮我写的、我怎么验证它的"**。

这份文档就是那件事的实践。与其说"这些代码全是我自己写的"，不如把**协作方式、AI 给了多少、我补了什么、哪一步卡住、错误类型怎么变**都摊开记下来 ——
面试官真正想知道的不是"你会不会用 AI"，而是**"你能不能判断 AI 给的东西对不对"**。

---

## 一、这是什么项目

一个学生的 **30 天 JavaScript + Node.js 实习冲刺**。

- **目标**：一个月后能独立交付"有数据库、有测试、已部署、接入 LLM 的 Node 后端服务"，并通过实习技术面
- **计划书**：`C:\Users\27971\.zcode\workspace\default\js-node-30day-plan.md`
  ⚠️ **注意：计划书在仓库外面**（不在 Git 里），所以它不会随仓库提交
- **仓库**：`C:\Users\27971\.zcode\workspace\default\js-node-30days`（GitHub: `xiaogumomo/js-node-30days`）
- **当前进度**：Day 7 进行中（**跨 9/19–9/20 两天**）；**结束日 10/15**

### 每天产出物在哪

```
js-node-30days/
  notes/
    dayNN.md                     每日日志（含「欠账登记」表）
    dayNN-<topic>.md             当天任务书（AI 写）
    dayNN-mistakes.md            错题本（Day 1 有）
  week1-language/
    dayNN-*.js                   当天练习 / 交付物
    dayNN-*-verify.js            验证脚手架（AI 写）
    p0-toolkit/
      src/*.js                   工具箱实现（学生写）
      test/*.test.js             工具箱测试（AI 写，node --test）
```

**命名是固定契约**：脚手架的 `require('./day0N-xxx.js')` 里写死了文件名，改名就找不到。

---

## 二、协作模式（最重要的部分）

### 每日循环

1. 学生说"帮我制定今天的计划" → AI 写**任务书**（`notes/dayNN-<topic>.md`）+ **日志模板**（`notes/dayNN.md`），并**更新计划书**
2. AI 写**验证脚手架**（读学生的实现文件，打印 `✅/❌` 并汇总）
3. 学生写实现 → **AI 实测**（跑脚手架、跑 `node --check`、抽段验证）→ 给逐行诊断
4. 学生改 → 再跑 → 直到脚手架全绿
5. **提交由学生自己做**（除非学生明确要求 AI 提交）

### 三条硬约定

1. **实现由学生写；测试可以由 AI 写**（学生计划书 §一 的红线原文："允许：让它写测试、写类型签名、写注释文档。不许让 AI 直接写出整个函数然后粘贴"）
2. **日志里学生写的内容不许删改** —— AI 的意见放在独立小节（`## AI 复核`）或 `>` 引用块里，并标明是 AI 补的
3. **AI 说的每个技术结论，先跑一遍再说** —— 已经犯过一次"没跑就断言"（我说"删掉 constructor 还是 4 行"，实测是 3 行），学生照着做会白费时间

### 两个容易踩的协作坑

- **不要为"还不存在的模块"提前写测试文件** —— 会让当前全绿的测试套变红（`node --test` 找不到模块直接报错）
- **`p0-toolkit/test/` 是项目资产（进 CI），`week1-language/*-verify.js` 是脚手架（即时反馈）** —— 两份并存，不冲突

---

## 三、错误类型演进与薄弱点

> 这一节是给下一个 AI 会话看的：**它决定"该给多少提示"**。写在这里是为了不用每次从零摸索。
> 对本人来说，这也是一份"我这一周的错误是怎么变的"的记录。

### 错误类型的演进 —— 这是最重要的诊断数据

| 阶段 | 错误类型 | 表现 |
|---|---|---|
| Day 4 `debounce` | **知识缺口** | `setTimeout` 没见过 → 直接卡死、无从下手 |
| Day 5 `throttle` 路线 B | **结构理解** | 代码放错位置（`return` 之后 / `if` 外面）→ 改 3 次 |
| Day 7 `myMap` | **手滑 / 不熟练** | 5 处 bug，**没有一处是"不懂"**（`>` 写成 `<`、`slice` 参数、`fn()` 没传参） |
| Day 7 零提示题 `dropNulls`（9/20） | **检索失败**（新类型） | 学过的 API 在"零提示"下取不出来：知识点 Day 3 排过、读过、清单打过勾，`for...in` 在 deepClone 里还用过 —— 零提示时却退化成"用数字下标 `obj[i]` 遍历对象" |

**结论：不懂的边界在收缩。** 当错误类型是"手滑"时，**给骨架不再有帮助** —— 需要的是"自己从零做一遍"。

**第三种类型（9/20 新增）：会，但取不出来。** 这是零提示题第一天就抓到的，也是最值得记的一条 —— 上面三种类型**要的介入方式完全不同**：

| 类型 | 表现 | 该怎么介入 |
|---|---|---|
| 知识缺口 | "这个我没见过" | 补章节（书单） |
| 结构理解 | "知道概念，但代码放错位置" | 改 + 讲解 |
| 手滑 | "都会，就是写错" | 让他自己重写，别给骨架 |
| **检索失败** | **"学过、用过，零提示下想不起来"** | **不讲概念、不给代码** —— 只指路到他自己的材料，然后要求"第二遍"（合上书）+ 隔一两天同类题再考 |

**判断顺序（下次遇到卡点照这个走）**：① 书单排过没有？→ ② 排过的话，学过之后有没有留下**能跑的东西**？（查清单的勾对不应对得上活代码）→ ③ 都对得上还卡住，才是真的知识缺口。

**⚠️ 一条跨学员/AI 的教训（9/21 发现）：别信"全绿"这个总数，要看每个文件里有几条真测试。**
`p0-toolkit/test/throttle.test.js` 从 Day 6 起**整份被注释掉**了，而 `node --test` 会把"没有测试的文件"也**算成 1 条通过** → `pnpm test` 一直显示全绿，**throttle 其实零覆盖**；我（AI）连续几轮引用的"22 条全绿"里就含着这条**幽灵测试**，我写的复习脚手架也因此给过 throttle 一个**假绿（1/1）**。
已处理：① 测试文件恢复（5 条，含学生自己补的一条），在真实实现上 **5/5 通过** → 工具箱现在 **26 条真测试**；② 复习脚手架加了**防假绿守卫**（判据文件里 0 条 `test()` 就报"这个绿不算数"并退出码 1）。
**给下一个会话的规矩**：报"测试全绿"之前，先跑一次"每个测试文件有几条真测试"（`for f in test/*.test.js; do grep -c "^test(" $f; done`）。**数字对不上就是有问题。**

`dropNulls` 那次的答案：① 排过（`notes/day03-arrays.md` 书单第 3 条《Object.keys，values，entries》）② **勾打了，但那一项的代码整段还注释着**（`day03-array-methods.js:190-201`）—— 所以是第三种：学过，没长在身上。

### 会反复出现的薄弱点（盯住）

| 薄弱点 | 证据 | 正确说法 |
|---|---|---|
| **`this` 的心智模型** | Day 6 小测 5 题暴露 | 习惯用"作用域查找 / 调用点 / 数据传递"解释。正确：**`this` 是调用时调用方塞进来的隐藏参数；箭头函数例外，它认定义位置** |
| **真假值判断** | `text = text \|\| "empty"`（Day 4）、`if(init)`（Day 7）—— **同一个坑踩了两次** | 判断"有没有传 / 是不是空"要用 **`=== undefined`**，不能用真假值 |
| **`node --check` 的习惯** | 语法错误跨了两个会话（`this speed = 0`），Day 7 又撞一次（`,,`） | 已写进计划书 §七 心法第 7 条："**追加代码前先 `node --check`**" |
| **自己搭判据（验证自己）** | Day 7 学生原话"**我验证不了是否正确**" —— 这一周的判据（脚手架、测试）**全是 AI 写的**，他一次都没自己搭过 | 所以遇到"对不对"，他的默认动作是**问 AI**，而不是**造一个小实验**。而计划 §六 附录 D 的自检原文就是"**能说清我的代码里哪部分是 AI 帮我写的、我怎么验证它的**"（面试会问） |
| **模块"接线"的习惯**（命名 / 导出 / 作用域） | Day 7 的 `day07-promise.js` 花了 **5 轮**，其中**大部分不是学新东西**，而是接线问题：同名函数互相覆盖（`buyfood`/`eat`/`login` 各两份，"哪一版生效"要靠猜，实测还真的把 test 3 覆盖成红的）、`p` 写在了作用域外、`eat` 忘了 `return`、两个 `login` 撞名。**这些返工占掉了当天的不少时间，而且会伪装成"今天进度慢"。** | 三条零成本规则：① **每段/每版用不同的函数名**，别靠"最后声明生效"；② `module.exports` 只放契约里那几个名字，其余别导出；③ **写完一个文件立刻跑一次**（心法第 7 条）——这 5 轮里有一半是一跑就现形的。**压掉返工比延长工时更能加快总进度。** |
| | | **怎么练（已排进周复盘日）**：周自测改成"给需求 → **他自己写判据** → 再自己写实现"，判据只看两件事：跑起来有没有预期输出、能不能数出行为（次数/时序）。别一上来就要求写完整测试文件 |

### 反馈方式上的三个注意点

- 会主动说"我不会 / 这条我没学过 / 我没实感" —— **这是优点，要鼓励**（知道自己哪块是空的）
- 但容易把**流程问题**误认为**能力问题**（说"我学得慢"）。遇到这种要**把两者分开讲**：`node --check` 这种是零学习成本的流程问题，不是能力问题
- 偏好**"先看到代码再读懂"**（原话："我需要的是看到你的代码再进一步读懂"）。所以"给骨架 + 填皮肉"是可接受的起点，**但要逐步撤支架**

---

## 四、已经确立的方法论（不要推翻）

1. **脚手架模式**：AI 写验证脚本（`console.log` + 自己数 pass/fail + `process.exit`）→ 学生裸写实现 → 脚手架给即时反馈
2. **"分级测试 + 探针"结构**：必过的判对错；**设计选择**（`throttle` 的 leading/trailing、`fn.length` 的陷阱、`reduce` 空数组）用"探针"**只记录不判错**，让学生自己看清自己在选什么
3. **计划书 §七 的 7 条心法**：卡住 20 分钟换路子 / 每天必须有一个能跑的东西 / 笔记只记三样 / 允许落后一天 / 第 2、4 周各一个"讲解日" / 清单优先探索靠后 / **每个文件写完立刻 `node` 跑一次**
4. **日程策略 = "选 A"（已拍板）**：**内容不砍、日期跟着实际走。** 每天只放 **3~4 小时**的量、把清单划干净，多出来的推给下一天。
   依据：仅有的两个 100% 完成日（Day 4、Day 5）**正是仅有的两个被刻意排轻的日子** —— 所以问题不是"学得慢"，是无排轻的日子按约 9 小时设计。
5. **欠账登记制**：日志里有「欠账登记」表；计划书 §四 有「进度看板与再计划机制」，规则是**累计欠账 ≥ 1 天就必须动结束日**
6. **"探索 TODO 停车场"**：学生想探索的东西先写进日志的 TODO 列表，不许插队（Day 2 跑题就是没这条纪律）

---

## 五、"书单缺口"清单（同一类问题，已记进计划书 §四）

javascript.info 的书单按"**知识点**"排，**反复漏掉"工具性章节"**。已经补/待补的：

| 缺口 | 什么时候发现的 | 处理 |
|---|---|---|
| 《调度：setTimeout 和 setInterval》 | Day 3 `debounce` 卡死 | 已补 |
| 《装饰器模式和转发，call/apply》（官方任务就叫「防抖装饰器」「节流装饰器」） | Day 3 | 已补 |
| 《递归和堆栈》 | Day 4 `deepClone` | 已补 |
| **CommonJS 的用法**（`module.exports` / `require`） | Day 5 建工具箱时已在用，却从没学过 | 已划进 Day 7 |
| **`Object.create` / ES5 手写继承** | Day 6 手写原型继承 | javascript.info **刻意不教**（被 `class` 取代）。仍值得做，理由是"面试会问 + 读得懂老代码" |
| **JSON 方法** | Day 6 测试打印 | 排进 **Day 9**（25 分钟小补） |
| **`process.nextTick` / 事件循环的阶段与各阶段队列**（第 7 个） | Day 8 做"微任务 vs 宏任务"预测题时，学生明确说"不知道 `process.nextTick` 是什么、不知道 `setTimeout` 属于 timers 队列、不知道'事件函数'是什么" | **这不是他的缺口，是排期问题**：这些属于 **Day 9「Node 架构与事件循环」**（计划里写着"事件循环六个阶段、`process.nextTick` 优先级、libuv 线程池"），而 Day 8 的任务书先拿 `process.nextTick` 做了预测题。**处理：保留这次"提前见一次"（间隔重复有益），Day 9 正式讲时必须回到 Day 8 那个现场**（`notes/day08-event-loop.js` 的 8 个标记顺序 + 他的三个"不知道"） |

> **规律：凡是学生说"这个我没见过"，先怀疑书单，而不是怀疑他。**
>
> **⚠️ 补充（9/20，零提示题第 1 道的教训）：查书单之前，先问两件事 —— ① 排过没有？② 排过的话，学过之后有没有留下"能跑的东西"？**
> 第一次遇到 `dropNulls` 卡"对象怎么遍历"，第一反应就是"书单漏了这一章" —— 查下来**排过**（Day 3 书单第 3 条）、**勾也打了**，只有那一项的代码**整段还注释着**。所以这次**不是**书单缺口，是"学过但没长在身上"（见第三节的第四种错误类型）。
> 两种情况的处理完全不同：**书单缺口 → 补章节；学过没长在身上 → 第二遍 + 隔天同类题再考**。别一遇到卡点就改书单。

---

## 六、下一步（9/23 = Day 9：先补 3 项欠账，再做事件循环）

**开工顺序（约 3.5–4 小时）**：

1. **补 3 项欠账（约 1 小时）**
   - **⑤ 回想三格**：`node week1-language/recall-verify.js` 跑 `curry` / `deepClone` / `throttle`（工具 9/23 已适配 `.ts`；`debounce` 那天已做：1/3 → 修一个 typo → 3/3）
   - **零提示题第 3 道** `flatOnce`（只给题目 + 验收标准，不给骨架）
   - **"TS 类型快查表" 20–30 分钟** —— 接住他 9/22 的卡点 #1：**"TS 中很多类型的表示方法不知道，一旦用不了 any 就卡住"**（这是"资料缺口"的第 8 例：TS 类型语法从没系统排过，全靠遇到一个学一个）
2. **Day 9 正式内容：Node 架构与事件循环** —— **必须先回到 9/21 的现场**：`week1-language/day08-event-loop.js` 的 8 个标记顺序 + 他答错的 `.then` vs `await` 对照实验 + 他明确说的三条"不知道"（`process.nextTick` 是什么、`setTimeout` 属于 timers 队列、"事件函数"是什么）。
3. 日志 + commit + push

**9/22–9/23 这两天的关键结果（详见 `notes/day08.md` 的「第 2 天」段）**：
- ✅ **TypeScript 转换全部完成**：`src` 5 个模块 + `test` 5 个文件全转 `.ts`；`pnpm test` **26 条全绿**、`pnpm exec tsc --noEmit` **零报错**（这次是真的 —— 10 个文件都在 `include` 里）。
- ✅ 顺延的 ③ `Promise.all` 家族、④ ESM vs CJS 笔记也补完了（ESM/CJS 笔记是他查着资料写的，按"**AI 辅助、不算掌握**"记账 → 在「忘了的」清单 #23–#25）。
- ⚠️ **字符级手滑累计 7 次**（第 7 次是 `cleaarTimeout`，害得 debounce 的 recall 判据 1/3）。**这是目前最值得盯的习惯问题** —— 对策仍是"写完立刻跑"。
- ⚠️ **`debounce` 的 immediate 语义他记成了另一种**（他的版本"每次都立刻执行" vs 工具箱的"leading + trailing，delay 内忽略后续"）→ 而**工具箱那条判据覆盖不到这个差异**（只测了第一次调用）→ 请他在 9/23 之后自己判断"是记错还是手滑"。

**⚠️ 给下一位会话的环境事实（能省半小时，都已实测）**：
- `pnpm add -D typescript @types/node`（**`@types/node` 必须装**，否则 `require` / `module` 报 `Cannot find name`）。
- **TS 7.0.2 删掉了 `moduleResolution: "node"`**（报 `TS5108`）→ tsconfig 用 `module: "nodenext"` + `allowImportingTsExtensions: true` + `types: ["node"]`。
- `node 文件.ts` 能直接跑（Node 24 类型剥离）、`node --test` **会自动发现 `*.test.ts`**、`require('../src/x.ts')` 也能用 → **不需要 Vitest / tsx**（Vitest 挪到第 3 周）。
- 递归函数报 `TS7023/TS7024` 时要显式写返回类型（"递归藏在回调里"才报，纯自递归 `return f(x)` 反而不报）。
- ⚠️ **`package.json` 被追加过尾部垃圾**（`tsc` 报 `trailing characters at line 15 column 1`，但**看起来像源码文件的错**）→ 报错里的文件名可能只是"被牵连的"，要顺着命令查真正的源头。**JSON 只能有一个顶层 `{}`。**

**记账（AI 侧，按计划 §一 红线）**：9/22 的 `curry` 转 TS 是 AI **走查示范**了一遍（第一次接触 TS 操作），其余 4 个模块由他自己转；`debounce` 那个 immediate bug 的"修法"也曾由 AI 给出（后经实测确认他原版其实是对的 —— AI 那次误报已记进日志）。

---

## 七、最新一条建议（**已落地**，2026-09-20 起执行）

**学生的原问题**："我这种抄抄问问，真正有效果吗？我没实感，骨架大部分是你给的。"

**诊断**：有效果（证据是错误类型从"知识缺口"退到"手滑"），但**缺一块 —— 从来没有在"完全没有提示"的情况下独立把一个问题拆成步骤**。而**面试就是那个场景**。

**三个补法（成本都很低），现在落地情况：**

1. **每天加一道"零提示题"（15–20 分钟）** —— ✅ **已落地**：写进了计划 §七 心法第 8 条，第 1 道题在 `notes/day07-day2.md` 的 ②。
   规则：只给**题目 + 验收标准**，不给骨架、不给状态表；卡住**只许查 MDN / 书，不许问 AI**；限时 20 分钟，到点停手（**产出是"看清卡在哪"，不是"写出来"**）；**问 AI 只许问"我卡在哪一步"，不许问"怎么写"**。
   第 1 道是 `dropNulls`（去掉值为 `null` 的键）—— 故意用 `0`/`''`/`false` 当陪练，因为**真假值判断这个坑学生已经踩过两次**（Day 4 的 `text || "empty"`、Day 7 的 `if(init)`）。候选题库：返回数组里最大的数 / 把嵌套数组拍平一层 / 统计字符串里每个字符出现的次数。
2. **逆向练习（5 分钟）** —— ✅ **已落地**：脚手架 `week1-language/day07-recall-verify.js`（默认读 `day07-recall-debounce.js`，可传路径参数复用）。
   规则：合上代码，**凭记忆重写**一个已经写好的函数。写不出来的地方就是真正不会的地方。（学生 Day 3 无意中做过一次 —— `pow`/`factorial` 的"第二遍复习"，方向是对的。）
3. **第 2 周起启动 LeetCode**（计划书里本来就有）—— ⬜ 还没到，第 2 周记得提。

---

## 八、给新会话的开场白建议

学生可以在新会话里直接说：

> 读一下 `notes/HANDOFF.md`，然后继续这个项目。今天是 9/20，Day 7 第 2 天。

或者（作为备份，如果文件读不到）：

> 读一下会话 `sess_0b328783-cf26-4840-b6e9-e5bb6ca7fe82` 的交接上下文，然后继续。
